# MongoDB connection string (Atlas recommended - this is what makes storage persistent on Render)
MONGO_URI=mongodb+srv://nani68629_db_user:NANI68629@zipsharedb.mzr1xxg.mongodb.net/zipshare?retryWrites=true&w=majority

# A long random string used to sign session cookies (generate with: openssl rand -hex 32)
SESSION_SECRET=change-this-to-a-long-random-string

# The ADMIN password, but hashed with bcrypt - never store it as plain text.
# Generate this by running:  node scripts/hash-password.js YourRealPassword
# Then paste the output below.
ADMIN_PASSWORD_HASH=N@ni6829
# Port (Render sets this automatically, this is just a local fallback)
PORT=5000

# Comma separated list of origins allowed to call the API (leave blank to allow same-origin only)
ALLOWED_ORIGINS=
